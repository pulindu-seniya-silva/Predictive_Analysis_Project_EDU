#import data set

data <- read.csv("data/xAPI-Edu-Data.csv")
data
colnames(data)

# Preview data
head(data)

# Load required libraries
library(dplyr)

# View structure
str(data)

# Preview data
head(data)

#Check Data Quality (Before Cleaning)
# Check dimensions
dim(data)

# Check column names
colnames(data)

# Check missing values
colSums(is.na(data))

# Check duplicates
sum(duplicated(data))


#Handle Missing Values & Duplicates
# Remove duplicate rows (if any)
data <- distinct(data)

# Remove rows with missing values (safe approach for this dataset)
data <- na.omit(data)

#Correct Data Types
#Convert categorical variables → factors
data$gender <- as.factor(data$gender)
data$NationalITy <- as.factor(data$NationalITy)
data$PlaceofBirth <- as.factor(data$PlaceofBirth)
data$StageID <- as.factor(data$StageID)
data$GradeID <- as.factor(data$GradeID)
data$SectionID <- as.factor(data$SectionID)
data$Topic <- as.factor(data$Topic)
data$Semester <- as.factor(data$Semester)
data$Relation <- as.factor(data$Relation)
data$ParentAnsweringSurvey <- as.factor(data$ParentAnsweringSurvey)
data$ParentschoolSatisfaction <- as.factor(data$ParentschoolSatisfaction)
data$StudentAbsenceDays <- as.factor(data$StudentAbsenceDays)


#Convert numerical columns properly
data$raisedhands <- as.numeric(data$raisedhands)
data$VisITedResources <- as.numeric(data$VisITedResources)
data$AnnouncementsView <- as.numeric(data$AnnouncementsView)
data$Discussion <- as.numeric(data$Discussion)

#define target variables
data$Class <- as.factor(data$Class)

# Check levels
levels(data$Class)

#Basic Consistency Checks
# Summary statistics
summary(data)

# Check value ranges (important for viva)
range(data$raisedhands)
range(data$VisITedResources)
range(data$AnnouncementsView)
range(data$Discussion)

# Final structure check
str(data)

# Confirm no missing values
sum(is.na(data))

data$Class <- factor(data$Class, levels = c("L", "M", "H"))


#Feature Engineering (for Classification Model)
#Create modelling data set

model_data <- data %>%
  select(raisedhands,
         VisITedResources,
         AnnouncementsView,
         Discussion,
         StudentAbsenceDays,
         Class)

#check target variables again
str(model_data$Class)
levels(model_data$Class)

#Encode Categorical Variable
model_data$StudentAbsenceDays <- as.factor(model_data$StudentAbsenceDays)

#create dummy variables for regression
model_data <- model.matrix(Class ~ ., data = model_data)[,-1] %>%
  as.data.frame()

# Add target back
model_data$Class <- data$Class

#create engagement score
model_data$EngagementScore <- 
  data$raisedhands +
  data$VisITedResources +
  data$AnnouncementsView +
  
#str (model_data)
  summary(model_data)
  

# For multinomial logistic regression
install.packages("nnet")

# For decision tree
install.packages("rpart")
install.packages("rpart.plot")

# For evaluation
install.packages("caret")

install.packages("rpart.plot", dependencies = TRUE)

# Install all packages with dependencies
install.packages(c("nnet", "rpart", "rpart.plot", "caret"), dependencies = TRUE)

library(nnet)       # Multinomial logistic regression
library(rpart)      # Decision tree
library(rpart.plot) # Plot decision tree
library(caret)      # Model evaluation


 
# For multinomial logistic regression
library(nnet)

# For decision tree
library(rpart)
library(rpart.plot)

# For evaluation
library(caret)


#Train-Test Split
set.seed(123)

train_index <- createDataPartition(model_data$Class, p = 0.7, list = FALSE)

train_data <- model_data[train_index, ]
test_data  <- model_data[-train_index, ]



